# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Provider
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: address
      external_name: address
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: bio
      external_name: bio
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: category
      external_name: category
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: city
      external_name: city
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: createdOn
      external_name: createdOn
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: email
      external_name: email
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: firstName
      external_name: firstName
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: lastName
      external_name: lastName
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: phone
      external_name: phone
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: postalCode
      external_name: postalCode
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: postfix
      external_name: postfix
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: prefix
      external_name: prefix
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: quote
      external_name: quote
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: region
      external_name: region
      # Type: STRING Stats: 0 properties of this type in this kind.

    - property: specialty
      external_name: specialty
      # Type: STRING Stats: 0 properties of this type in this kind.


    - property: startYear
      external_name: startYear
      # Type: STRING Stats: 0 properties of this type in this kind.



- kind: Schedule
  connector: csv
  connector_options:
    encoding: utf-8
    columns: from_header
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: day
      external_name: day
      # Type: Integer Stats: 10 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: endTime
      external_name: endTime
      # Type: Integer Stats: 10 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: provider
      external_name: provider
      # Type: Key Stats: 10 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Provider')
      export_transform: transform.key_id_or_name_as_string

    - property: startTime
      external_name: startTime
      # Type: Integer Stats: 10 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

